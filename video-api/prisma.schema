# Copy schema from main project
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}

# Note: This is a symlink to the main Prisma schema
# The actual schema is at ../prisma/schema.prisma
